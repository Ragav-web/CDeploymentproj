apiVersion: v1
kind: Service
metadata:
  name: helloapp
spec:
  selector:
    app: helloapp
  ports:
    - protocol: TCP
      port: 85          # Exposed service port
      targetPort: 3000  # Container port
  type: ClusterIP

